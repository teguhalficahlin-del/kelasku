# Toolchain test — tidak pernah ikut ke repo.
node_modules/
playwright-report/
test-results/
blob-report/
tests/.auth/
.env
.env.local

# Sisa "deno check" saat memeriksa Edge Function — versi Deno di CI ditentukan
# Supabase, bukan berkas ini.
deno.lock

# Sisa alat bantu OS dan editor
Thumbs.db
.DS_Store
~$*
